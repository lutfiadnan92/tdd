describe("substraction function", () => {
  test("test with positive value", () => {
    const a = 1
    const b = 1
    const result = a-b
    const output = 0
    expect(result).toEqual(output)
  })
})
